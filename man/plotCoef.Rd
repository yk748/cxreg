% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCoef.R
\name{plotCoef}
\alias{plotCoef}
\title{Plot coefficient paths from a classo fit}
\usage{
plotCoef(
  beta,
  norm,
  lambda,
  df,
  dev,
  label = FALSE,
  xvar = c("norm", "lambda", "dev"),
  xlab = iname,
  ...
)
}
\arguments{
\item{beta}{A complex-valued coefficient matrix of dimension
\code{nvars x nlambda}, as stored in a \code{"classofit"} object.}

\item{norm}{Optional numeric vector of L1 norms to use as the x-axis when
\code{xvar = "norm"}. If missing, computed as \code{colSums(Mod(beta))}.}

\item{lambda}{Numeric vector of lambda values from the fitted model.}

\item{df}{Integer vector of nonzero coefficient counts at each lambda.}

\item{dev}{Numeric vector of deviance ratios at each lambda.}

\item{label}{Logical. If \code{TRUE}, annotate the curves with the original
variable indices at the end of the path. Default \code{FALSE}.}

\item{xvar}{Character string selecting the x-axis scale: \code{"norm"} for
the L1 norm, \code{"lambda"} for \code{log(lambda)}, or \code{"dev"} for
the fraction of deviance explained.}

\item{xlab}{X-axis label. Defaults to a label matching \code{xvar}.}

\item{\dots}{Additional graphical arguments passed to \code{matplot}.}
}
\value{
\code{NULL}, invisibly. Called for its side effect of producing a
two-panel plot.
}
\description{
Internal function called by \code{\link{plot.classo}} to produce the
coefficient profile plot. Draws two panels: one for the real parts and one
for the imaginary parts of the complex-valued coefficient paths.
}
\keyword{internal}
